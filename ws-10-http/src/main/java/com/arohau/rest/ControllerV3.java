package com.arohau.rest;

public class ControllerV3 {
    // multiple entrances to the resources - verb are used - further options provided
    public Object getData(Object request) {
        Object response = new Object();
        // add possible manipulation links to response
        return response;
    }

    // multiple entrances to the resources - verb are used - further options provided
    public Object postUpdateData(Object request) {
        Object response = new Object();
        // add possible manipulation links to response
        return response;
    }

    // multiple entrances to the resources - verb are used - further options provided
    public Object updateData(Object request) {
        Object response = new Object();
        // add possible manipulation links to response
        return response;
    }

    // multiple entrances to the resources - verb are used - further options provided
    public Object deleteData(Object request) {
        Object response = new Object();
        // add possible manipulation links to response
        return response;
    }
}
